{
  "name": "visual-resume-editor",
  "version": "1.0.0",
  "description": "A simple visual resume editor — DevOps CI/CD showcase project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "jest --forceExit --detectOpenHandles",
    "lint": "eslint . --ext .js --ignore-pattern node_modules/ --ignore-pattern dist/",
    "build": "node scripts/build.js"
  },
  "keywords": ["devops", "jenkins", "docker", "ci-cd", "resume-editor"],
  "author": "Sahil",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
